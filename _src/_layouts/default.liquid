<!DOCTYPE html>
<html>
    <head>
        {% include 'head.liquid' %}
        <title>{{ page.title }}</title>
    </head>
    <body>
    {% include 'navbar.liquid' %}

    <div class="main">
      {{ page.content }}
    </div>

    {% include 'footer.liquid' %}
  </body>
</html>
